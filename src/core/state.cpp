// core/state.cpp — everything in state.h is currently inline. Reserved for
// the bigger transition table that lands with the menu / pause / help
// states in P8.

#include "core/state.h"

namespace core {
// (intentionally empty)
} // namespace core
