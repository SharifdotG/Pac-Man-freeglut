// world/tile.cpp — everything in tile.h is constexpr/inline. This TU exists
// only so CMake's per-module .cpp list stays uniform; out-of-line helpers
// land here when they're added.

#include "world/tile.h"

namespace world {
// (intentionally empty)
}  // namespace world
